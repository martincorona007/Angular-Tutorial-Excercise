import { TextovozDirective } from './textovoz.directive';

describe('TextovozDirective', () => {
  it('should create an instance', () => {
    const directive = new TextovozDirective();
    expect(directive).toBeTruthy();
  });
});
