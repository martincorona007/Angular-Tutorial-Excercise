import { SiDirective } from './si.directive';

describe('SiDirective', () => {
  it('should create an instance', () => {
    const directive = new SiDirective();
    expect(directive).toBeTruthy();
  });
});
